<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="style_common">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/dp_10</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="style_common_et" parent="style_common">
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="style_common_btn" parent="style_common">
        <item name="android:layout_margin">@dimen/dp_5</item>
    </style>

</resources>
